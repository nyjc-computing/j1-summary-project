import random


def pull(n: int) -> str:
    """Takes an integer n and rolls that number of times, returning a string name of the item/character received.""" 
    pass
def fear(n: int) -> str:
    pass